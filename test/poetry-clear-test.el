;;; poetry-clear-test.el --- Tests for poetry.el


;; (ert-deftest poetry-clear-should-clear-cache ()
;;   (let* ((ppath (poetry-test-create-project-folder))
;;          (default-directory ppath))
;;     ;; just test that it is not erroring
;;     (poetry-clear)))

;;; poetry-clear-test.el ends here
